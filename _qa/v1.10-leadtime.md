# v1.10 — Menubar UI · lead-time

| Marker | Unix ts | Local (-03) |
|---|---|---|
| Start (first Bash call) | 1780527256 | Wed Jun  3 19:54:16 2026 |
| Commit | 1780527953 | Wed Jun  3 20:05:53 2026 |
| **Delta** | **697 s** | **~11 min 37 s** |

Single-session ship: Swift Package scaffold + buildable target + SwiftUI menubar (NSStatusItem + popover + queue poll + Skip/Clear + voice picker) + protocol parser smoke runner + docs (changelog + roadmap + new menubar.md + arquitetura mention + whats-next removal + sidebar entry) + version bumps (zig 1.10.0 + zon 1.10.0).

Hard gates green at commit time:
- `zig build` ✅
- `zig build test` ✅ (existing tests untouched)
- `cd ui/menubar && swift build -c release` ✅ (321 KB binary, Swift 6.3.2)
- `swift run -c release SocketProtocolCheck` ✅ (13/13 parser assertions)

Honest scope deferred to v1.10.1:
- Volume ducking (CoreAudio + entitlement + signing)
- Linux GTK4 status icon
- Per-id skip (needs daemon `SKIP\t<id>\n` extension)
- Drag-to-reorder (needs daemon `MOVE` op)
- Signed `.app` + brew cask
- End-to-end smoke against a running daemon (`_qa/v1.10-baseline.md`)
