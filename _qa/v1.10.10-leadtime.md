---
version: v1.10.10
agent_start_ts: 1780571670
commit_ts: 1780572983
elapsed_seconds: 1313
elapsed_minutes_approx: 22
---

# v1.10.10 Lead-time

Scope: lock tight-narrator as `--profile tech` default + ffmpeg post-fx pipeline (off/clean/tech/broadcast) wired through ipc, queue, daemon, mcp, client.

- agent_start_ts: 1780571670
- commit_ts: 1780572983
- elapsed_seconds: 1313 (~22 min)

## Live validation captures

CLI smoke (postfx applied per chunk):

```
[worker] piper id=191 tech=true length_scale=1.050 noise_scale=0.350 noise_w=0.450 speaker_id=-1 sentence_pause_ms=500
[worker] id=191 chunk=0 postfx=tech postfx_ms=148.6 (>100ms — eating into TTFA)
[worker] id=191 chunk=1 postfx=tech postfx_ms=63.5
[worker] id=191 chunk=2 postfx=tech postfx_ms=53.3
[worker] piper id=191 streaming chunks=3 first_audio=291.3ms total=8559.4ms samples=173568
```

A/B: id=190 ran with `--postfx off` and emitted no `postfx=` lines (dry path, zero overhead).
id=191 ran with `--postfx tech` and ran each chunk through the research-anchored ffmpeg chain.

MCP `tech_profile_search` returned 8 ids (192-199), one per knob×postfx variant:

```
192 tight-narrator + postfx=off
193 tight-narrator + postfx=tech
194 stock-tech + postfx=off
195 stock-tech + postfx=tech
196 broadcast + postfx=off
197 broadcast + postfx=tech
198 expressive + postfx=off
199 expressive + postfx=tech
```

## Build + test status

- `zig build -Dwith-piper=true --release=safe` → exit 0
- `zig build test -Dwith-piper=true` → exit 0

## Conflict notes (vs v1.10.11 + v1.10.12 parallel worktrees)

- `/opt/homebrew/bin/agent-tts` got swapped mid-validation by a parallel worktree; reinstalled ours and re-kickstart cleared the race.
- v1.10.10 did NOT touch `src/piper.zig`, `src/ssml.zig`, or `src/audio.zig` — postfx routing lives entirely in the new `src/postfx.zig` + a daemon-side `playWithPostfx` helper. Merge with v1.10.11 / v1.10.12 should be clean on those three files.

## Honest scope

- ffmpeg cold spawn is ~80-200ms — the streaming pipeline absorbs it for long inputs (synth-per-chunk ~80-150ms) but short utterances pay the full overhead on TTFA.
- RNNoise model (`cb.rnnn`) is best-effort: when missing, the `tech` chain drops `arnndn=` and runs EQ+deesser+comp subset only.
- Missing ffmpeg → silent pass-through to dry PCM. The user sees no error; the log shows `passthrough (ffmpeg/model unavailable)`. Conscious choice.
