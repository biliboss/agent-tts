# agent-tts v1.10.2 lead-time

- agent_start_ts: 1780531830
- agent_start_iso: 2026-06-03 (start of session)
- commit_ts: 1780533112
- elapsed_seconds: 1282
- elapsed_minutes: 21m 22s

## Scope shipped end-to-end (live-validated)

- audio.zig pause/resume/is_paused + paused_at_ns latch + current_sound atomic slot
- ipc.zig PAUSE/RESUME/REPLAY/HISTORY ops, 8 new parser tests, backward-compat preserved
- queue.zig history() + replay() with `finished_at` column
- daemon.zig current_playing_id atomic + handleClient dispatch
- client.zig 4 new subcommands + 4 silent helpers
- mcp.zig tools list 6 → 10 (pause/resume/replay/history), VERSION 1.10.2
- ui/menubar FloatingPlayer NSPanel + AppDelegate polling + Settings toggle
- bundle CFBundleShortVersionString 1.10.2
- docs: changelog, menubar (floating section), mcp (10 tools), arquitetura (polling + ops)
- npm run build clean

## Live validation results

- `zig build -Doptimize=ReleaseFast -Dwith-piper=true` → exit 0 (1.3 MB binary)
- `zig build test` → exit 0
- `swift build -c release` (menubar) → exit 0
- `swift run -c release SocketProtocolCheck` → 17/17 ok (4 new assertions)
- Binary installed `/opt/homebrew/bin/agent-tts` + codesign + `launchctl kickstart -k`
- `agent-tts pause` while id=100 plays → daemon ack `paused id=100`, audio actually halted
- `agent-tts resume` → daemon ack `resumed id=100`, audio resumed from same cursor
- `agent-tts history --limit 5` → 5 rows printed with finished_at populated
- `agent-tts replay 99` → new id=101, immediately playing
- `agent-tts replay 999999` → `ERR\titem not found`, exit 1
- MCP `tools/list` over stdio → 10 tools listed in order
- MCP `tools/call pause` while id=102 plays → `{"paused_id":102}` (isError=false)
- Menubar `.app` 1.10.2 installed in /Applications/
- Floating widget enabled via `defaults write` + app restart → panel appeared above all windows during playback
- Screenshot captured `_qa/v1.10.2-floating-player-full.png` showing widget visible
