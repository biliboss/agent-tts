# v1.10.3 lead-time

- agent_start_ts: 1780533322 (2026-06-03 21:35 UTC-3)
- goal: guided voice clone UI in menubar (one-button)
- commit_ts: 1780534421 (2026-06-03 21:53 UTC-3)
- elapsed_seconds: 1099 (~18 minutes 19 seconds)

## Breakdown

- 0 – 4 min: read worktree, baseline build (libpiper symlink), baseline tests.
- 4 – 8 min: zig --quiet flag + voice.zig HELP tests + version bumps.
- 8 – 14 min: VoiceRecorder.swift (AVAudioRecorder, peakLevel, permission), CloneVoiceWindow.swift (520x640 NSWindow + SwiftUI root, 5-sentence script, VU meter, Process spawn).
- 14 – 17 min: AppDelegate openCloneWindow wiring + QueueView popover row + bundle plist (NSMicrophoneUsageDescription).
- 17 – 18 min: live-test (window opened, mic granted, 40s recording produced, --quiet round-trip validated via stub sidecar), docs (changelog + menubar + whats-next).

## Live test result

- Clone window: opened via `AGENT_TTS_AUTOSHOW_CLONE=1 open`.
- Mic permission: prompted + granted on first launch (cached after).
- Recording: 40.4 s captured at 22050 Hz mono s16le = 1.8 MB WAV at `/var/folders/.../T/agent-tts-clone-<UUID>.wav` → staged to `~/.cache/agent-tts/voices/.tmp-gabriel.wav`.
- Subprocess: `/opt/homebrew/bin/agent-tts voice clone --sample <wav> --name <slug> --quiet` spawned correctly (visible in log textbox). With the unpatched installed v1.10.2 binary it (correctly) rejected `--quiet`; after installing the v1.10.3 binary the contract round-trips.
- `--quiet` contract: validated independently against a stub sidecar — produces exactly `OK\tunit-test-quiet\n` (exit 0). Verbose mode regression-checked.
- New voice in picker: not exercised end-to-end this session because the XTTS sidecar `.venv-voice` was not provisioned; the catalog-reload code path is unchanged from v1.10 and is exercised on every popover open.

## Honest scope

- Live XTTS sidecar produced no real embedding this session (no `.venv-voice`). Users with `scripts/setup-voice-clone.sh` already run get the real path. See changelog for the disclosure.
- The `AGENT_TTS_AUTOSHOW_CLONE` env hook is intentional + production-safe (no-op without the env var).
